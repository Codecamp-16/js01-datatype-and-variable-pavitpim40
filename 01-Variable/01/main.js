// - ให้ประกาศตัวแปรชื่อ person และ name
// - ใส่ชื่อตัวเองลงในตัวแปร name
// - นำค่าที่อยู่ในตัวแปร name ไปใส่ให้ (assign) person
// - เมื่อ alert(person) ออกมาต้องเป็นชื่อตัวเอง

// keyword var,let,const

var person;
var name;

name = 'pavit';

person = name;
// person = "pavit"

alert(person);
